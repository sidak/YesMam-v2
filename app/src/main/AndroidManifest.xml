<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sidak.yesmamv2" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Control.ClassesListActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".Control.CircleActivity"
            android:label="@string/title_activity_circle" >
        </activity>
        <activity
            android:name=".Control.SplashScreenActivity"
            android:label="Yes Mam " >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
